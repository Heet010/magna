# Copy this file to `.env` and fill in your key.
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional: override the model. gpt-4o-mini = cheap/fast (default);
# gpt-4o = more accurate for the numbers (recommended for the final run).
# AGENT_MODEL=gpt-4o
